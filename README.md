# FreqTrade Bot

### Getting Started

1. Ensure Docker is installed.
2. Configure your config.json file (copy from config.example.json).
3. Run the bot using the command:
```
docker compose up -d
```

> This project is intended for Dry-Run mode only.